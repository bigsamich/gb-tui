/target
test-roms/pokemon-red.gb
*.sav
*.st[1-4]
run/
run/maps/
gb-tui.toml
journal/

# python caches
__pycache__/
*.pyc

# per-game local artifacts (training-data pools, fleet logs) — knowledge/ IS tracked
games/*/data/
games/*/data_demos/
games/*/autoplay_runs/
games/*/rollouts/
games/*/scenarios/

# third-party walkthrough extractions (copyrighted guide text) stay LOCAL only —
# the grounded structure we derive lives in the tracked subgoals.json instead
games/*/knowledge/walkthrough/

# heavy training artifacts (venv, llama.cpp build, merged models, ggufs) — never pushed
training/.venv/
training/llama.cpp/
training/runs/
models/
